# GDES v1.0.1

Local-first CLI for LLM artifact management.

Usage:
  echo "code" | python -m gdes pipeline --stdin -c my_tool -t code
  python -m gdes status
  python -m gdes search --concept my_tool --json
